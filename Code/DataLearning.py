import pandas as pd

def LearnAndTest(data:pd.DataFrame) -> None:
    print("Beginning the process of learning the data...")

    print("Completed the process of learning the data.")

# Look at HW #3
def __knnLearning(data:pd.DataFrame) -> None:
    print("Performing KNN learning...")

    # Something probably needs to be returned for testing

# Look at HW #3
# Previously planning on using info gain, but realize since I want the tree anyways
# then using the decision tree model makes more sense.
def __decisionTreeLearning(data:pd.DataFrame) -> None:
    print("Performing Decision Tree learning...")
    # Train

    # Call for creating the trees

# Look at HW #3
def __testModels(data:pd.DataFrame) -> None:
    print("Beginning the process of testing the learned models...")

# Look at HW #2
def __correlationAnalysis(data:pd.DataFrame) -> None:
    print("Performing correlation analysis with x^2...")